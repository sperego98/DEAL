### N$_2$ example: DEAL results
This folder contains the results of running DEAL on the N$_2$ dataset.

The results are stored in the `selection/` folder.

To reproduce the results, you can run the provided Jupyter notebook `example-N2.ipynb`, or use the configuration file `input.yaml` with the command line interface:

```bash
deal -c input.yaml
```